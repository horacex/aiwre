name = "REPLACE_WITH_PRIVATE_PROJECT_NAME"
main = "REPLACE_WITH_PRIVATE_WORKER_ENTRY"
compatibility_date = "YYYY-MM-DD"

# Public repo template only.
# Internal deployment configuration is intentionally excluded.

[vars]
# Add private environment configuration in internal docs/repo.
