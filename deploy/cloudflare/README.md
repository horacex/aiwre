# Cloudflare Placeholder

This folder intentionally contains only redacted templates.

Public repository policy:
1. no deploy-ready Worker source
2. no deploy-ready `wrangler.toml`
3. no account/domain/queue/runtime operation details

Use `wrangler.toml.example` only as a non-sensitive scaffold.
